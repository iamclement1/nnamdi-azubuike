export type PortfolioItem = {
  id: number;
  name: string;
  uri: string;
}

export type ProjectDataType = {
  id: number;
  name: string;
  desc: string;
  action: string;
  uri: string;
}

export type NavLinks = {
  id: number;
  name: string;
  href: string;
}